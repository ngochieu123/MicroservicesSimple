spring:
  application:
    name: service-b
  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8080
management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0